{
  "name": "noc",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "dev": "tsnd --env-file=.env --respawn -r tsconfig-paths/register src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "keywords": [],
  "author": "Suleware",
  "license": "ISC",
  "description": "noc project",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "cron": "^3.1.7",
    "env-var": "^7.5.0"
  }
}
